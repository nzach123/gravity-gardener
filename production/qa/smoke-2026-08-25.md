# Smoke Check Report

**Date**: 2026-08-25
**Sprint**: 1
**Engine**: Godot 4.7.1-stable (GL Compatibility)
**Build**: branch `vertical-slice`, `df0c332`
**QA Plan**: `production/qa/qa-plan-sprint-1.md`
**Argument**: `sprint`
**Run by**: nzach123

---

## Precondition

The QA plan states: *"Run `/smoke-check sprint` once CLR-002's playtest passes —
not before. A smoke check that runs while the sprint's one open S2 bug is
unverified reports a build state that does not exist."*

Satisfied. CLR-002's playtest passed on 2026-08-25 and BUG-0001 is closed —
see `production/qa/evidence/bug-0001-killplane-playtest-2026-08-25.md`.

---

## Automated Tests

**Status: PASS**

| Metric | Value |
|---|---|
| Test cases | 178 |
| Passing | 178 |
| Errors | 0 |
| Failures | 0 |
| Flaky | 0 |
| Skipped | 0 |
| Orphans | 0 |
| Suites executed | 11 / 11 |
| Execution time | 38s 192ms |
| Exit code | 0 |

Command:

```
Godot_v4.7.1-stable_win64_console.exe --headless --path . \
  -s addons/gdUnit4/bin/GdUnitCmdTool.gd --ignoreHeadlessMode -a res://tests -c
```

The `-c` flag is required. Without it the runner stops at the first failure and
a red run under-reports.

This run was taken AFTER the `level_04.tscn` revert in `df0c332`, so it reflects
the tree as committed rather than the tree as played.

---

## Test Coverage

| Story | Type | Test file | Status |
|---|---|---|---|
| CLR-001, CLR-004 | Logic | `tests/unit/physics/collision_layers_test.gd` | COVERED |
| CLR-002 | Integration | `tests/integration/main/kill_area_death_test.gd` + playtest evidence | COVERED |
| CLR-003 | Integration | collision suite T3.1-T3.3 + playtest evidence | COVERED |
| CLR-005, TUN-006 | Logic | `.github/workflows/tests.yml` — the CI steps are the named artifact | COVERED |
| TUN-002, TUN-003 | Config/Data | — | EXPECTED |
| TUN-004, TUN-005 | Logic | `tests/unit/tuning/tuning_resources_test.gd` | COVERED |
| LV-001 - LV-004 | Logic | `tests/unit/level/level_validation_*_test.gd` (4 files) | COVERED |
| GA-001 | Logic | `tests/unit/gravity/gravity_authority_contract_test.gd` | COVERED |

**Summary**: 13 covered, 0 manual-only, **0 missing**, 2 expected.

---

## Manual Smoke Checks

Drawn from `tests/smoke/critical-paths.md`, scoped by the QA plan's Smoke Test
Scope section.

| Check | Description | Result |
|---|---|---|
| 1-3 | Core stability — launch, new session, menu input | PASS |
| 12-13 | Spike death at any gravity orientation; death restarts the level and resets watering/bucket state | PASS |
| 4-5 | Player move with acceleration/friction; jump with coyote time and buffering | PASS |
| 11 | Moving platforms carry the player in any gravity orientation | PASS |
| 14-15 | GameManager state reset on restart; per-level state across transitions | PASS |
| — | Save / load | **N/A** — no save system exists in the vertical slice |
| 16-17 | 60 FPS with no visible drops; no memory growth over 5 minutes | PASS |

### Why checks 4, 5, 11, 12 and 13 were run attentively

The QA plan flagged these as the sprint's only in-game risk surface:

- **12 and 13** share the death path CLR-002 repaired. Before this sprint the
  kill plane in levels 05 and 06 was structurally incapable of firing.
- **4, 5 and 11** cover exactly what CLR-003 deleted: the `PlayerArea2D` node and
  its `CollisionShape2D` from `player.tscn`, the dangling `col_shape` var from
  `player.gd`, and the `collision_mask = 2` line from `moving_platform.tscn`.

All passed.

---

## Missing Test Evidence

All Logic and Integration stories have test coverage. Nothing outstanding.

---

## Verdict: **PASS**

- Automated tests pass — 178/178, exit 0
- Every manual smoke check passes or is N/A
- No missing test evidence

The build is ready for manual QA hand-off.

---

## Notes for the QA cycle

Three Sprint-1 rows are still at `review`, none gated on a human:

- **CLR-005** and **TUN-006** — both need one scratch-branch CI run with a
  deliberate violation per check, confirmed red, then reverted. The workflow
  trigger was widened to `development` in `d77337c`; before that it targeted
  `main`, a branch this repository does not have, so no run had ever fired.
- **TUN-005** — its V1 note stands. Re-pointing a `.tres` at the wrong script is
  a parse error at load, not a group-1 failure, so V1 was never demonstrated
  red. Do not weaken V1 to a null check to make it observable.

`docs/tech-debt-register.md` holds 10 open advisory items — 3 from GA-001 and 7
from the CLR-004 code review. None block this gate.

One unrelated change was caught and reverted during close-out: `SpikeHazard3` in
`level_04.tscn` had moved from `Vector2(346, 30)` to `Vector2(346, -27)` during
the play session. Reverted in `df0c332`. The playtest sign-off is unaffected —
level 04 bore only on CLR-003's platform and player checks.
