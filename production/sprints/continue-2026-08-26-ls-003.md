# Continuation Prompt — written 2026-08-26 (end of the eighteenth session)

Paste the block below into a new session. Delete this file once it is consumed.

---

## Start here

Read `production/session-state/active.md` first, in full. Read the two most
recent Session Extracts at the bottom of it (LS-001 close, LS-002 close) before
anything else. Then re-derive git state yourself:

    git log -1
    git status --short

Do NOT trust any commit hash or file list written into a document. Earlier
versions of `active.md` were stale within a day of being written, and one of
them sent a session down a wrong path.

## Where things stand

**LS-002 (`OxygenState`) is CLOSED.** Complete, 8/8 acceptance criteria ticked,
suite green at 286/286 across 16 suites, exit 0. Do not reopen it.

**The Foundation level-state track is: LS-001 done → LS-002 done → LS-003 next.**

**Nothing is committed.** The working tree is dirty and holds every change from
the last three sessions. `.probe_tmp/` is present and disposable.

## The task

Run the standard story loop on **LS-003**:

    /story-readiness production/epics/level-state/story-003-frame-priority-contract.md

then `/dev-story`, `/code-review`, `/story-done` on the same path.

LS-003 is `ready-for-dev`, Must Have, estimated 0.13 days — the smallest
remaining row in the sprint. After it, LS-004 is the high-value one: it
constructs and injects both state objects, and it is what unblocks GA-005 and
what lets GA-004's wiring test become behavioural instead of source-text
matching.

## Carry these forward — they cost real sessions to learn

1. **Run `/story-readiness` before `/dev-story`, and believe it when it says
   NEEDS WORK.** LS-002 came back NEEDS WORK on a genuinely unsatisfiable
   acceptance criterion. Fixing it took three annotations and about two minutes.
   Implementing against it would have produced either a failing test or a
   silently wrong tick.

2. **When reality and an approved criterion diverge, ANNOTATE the criterion.**
   Never reword it, never silently tick it, never "fix" the code to chase a
   claim the engine disproves.

3. **An external write to a getter-only property is DISCARDED SILENTLY in
   4.7.1.** It raises nothing. Probed in four shapes; evidence at
   `production/qa/evidence/getter-only-assignment-probe-2026-08-26.md`. ADR-0002
   `:246-248` and `:291` both still claim otherwise and are both still wrong; an
   erratum row sits in `docs/tech-debt-register.md`. Assert unchanged values, and
   pair the assertion with a genuinely writable negative control so a vacuous
   pass cannot hide.

4. **Fix the decisions BEFORE dispatching an executor.** LS-002 went cleanly in
   one pass because all six open choices were decided in the brief. An executor
   given latitude spends it.

5. **Verify a subagent's numbers yourself.** Read `reports/report_NN/results.xml`.
   Do not relay a suite count you did not see.

6. **Check that a guard has teeth, not just that it is green.** LS-002's
   hardcoded-threshold guard matched `"0.50"` and `"0.10"` but not `0.5` and
   `0.1`, the spellings anyone would actually write. It passed against the
   violation it existed to catch. Ask of every structural test: what would the
   realistic violation LOOK like, and would this catch THAT?

7. **Open the config file before relaying a config-dependent severity.** Six
   findings in this project have invented one. The seventh, in LS-002, turned out
   to be real — which is exactly why the check has to keep happening.

## Two documents still carry a known-wrong claim

Neither blocks LS-003. Both should be fixed before anyone cites them.

- The AC-4 edge-case note in
  `production/epics/level-state/story-002-oxygen-state-object.md`
- The 2026-08-25 addendum in `production/qa/qa-plan-sprint-2.md`

Both say the exact-boundary side of the oxygen threshold bands is undecided and
must be picked by the implementer. It is not. `design/gdd/suit-oxygen.md:97`
specifies it verbatim: `nominal > 0.50 · caution ≤ 0.50 · warning ≤ 0.25 ·
critical ≤ 0.10`. The boundary belongs to the lower band. The shipped code
matches the GDD.

## Open decision for the developer

Nothing has been committed for three sessions. The tree holds LS-001, LS-002,
the GA-004 close, and several document updates. Consider committing before
starting LS-003, so a bad session cannot take all of it. **Never `git add -A`** —
it swept an unreviewed level edit into a docs commit on 2026-08-25. Stage named
paths and read the diff.

The separate, larger open question is still unanswered: merge `vertical-slice`
into `development`. It is 77+ commits ahead, and the merge is what puts the CI
guards on the trunk where they can actually run.

## The test command

    "/c/00_repos/00-Godot-installer/Godot_v4.7.1-stable_win64.exe/Godot_v4.7.1-stable_win64_console.exe" --headless --path . -s addons/gdUnit4/bin/GdUnitCmdTool.gd --ignoreHeadlessMode -a res://tests -c

Keep the `-c`. Without it a red run stops at the first failure and under-reports.
If it fails to load with `Could not find type "GdUnitTestCIRunner"`, the `.godot`
class cache is stale — run `--import` first, then retry. Expect **286/286 across
16 suites, exit 0** as the baseline.
