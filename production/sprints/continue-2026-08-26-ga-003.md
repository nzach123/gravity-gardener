# Continuation Prompt — Sprint 2 Implementation, GA-003 (2026-08-26, thirteenth session close)

Paste the block below into a fresh Claude Code session.

**Supersedes `continue-2026-08-25-qa-plan-sprint-2.md`**, which is fully consumed.
That prompt had two jobs: run `/qa-plan sprint`, and strike four stale QQ rows from
`architecture.md`. Both are done. Delete that file when you commit this one.

Three things changed on 2026-08-25 and 2026-08-26 that every earlier handoff gets wrong:

1. **`/qa-plan sprint` has been run.** The plan is at `production/qa/qa-plan-sprint-2.md`.
   Addendum blocks were appended to 14 story files.
2. **Sprint 2 implementation has started, ahead of the 2026-09-01 date.** GA-002 is
   Complete. Do not treat Sprint 2 as unstarted.
3. **The suite is 191/191 across 12 suites, not 178/178 across 11.** Every earlier
   prompt states 178. That number is now wrong.

---

## Copy from here

    Resume work on branch `vertical-slice`. Sprint 2 is in progress. GA-002 is
    Complete. The next story is GA-003.

    Ground yourself first. Do not trust any status you have not checked this
    session. Five handoff documents in this repo have now outlived the facts they
    asserted.

    1. Read `production/session-state/active.md`. Read the Session Extract blocks
       at the end first — they are the newest record.
    2. Read `production/qa/qa-plan-sprint-2.md`. This is new and is UNTRACKED.
    3. Read `production/sprint-status.yaml` — the live machine-readable rows.
    4. Read `production/sprints/sprint-2.md` — the plan you are executing.
    5. Run `git log --oneline -5` and `git status --short`.
    6. Run the suite and confirm green before you change anything:

       "/c/00_repos/00-Godot-installer/Godot_v4.7.1-stable_win64.exe/Godot_v4.7.1-stable_win64_console.exe" --headless --path . -s addons/gdUnit4/bin/GdUnitCmdTool.gd --ignoreHeadlessMode -a res://tests -c

       Expect **191/191, 12/12 suites, exit 0**. Keep the `-c`. Without it the
       runner stops at the first failure and a red run under-reports. Use
       `-a res://tests`, not `-a res://tests/unit` — the narrower path skips
       `tests/integration` and reports 183/11, which looks like a regression and
       is not one. If the runner fails to load with `Could not find type
       "GdUnitTestCIRunner"`, the `.godot` class cache is stale. Run `--import`,
       then retry.

    ## The first thing to decide: commit the backlog

    **Nothing from the last three sessions is committed.** 20 modified files and
    2 untracked files are in the working tree. They span three separate pieces of
    work. Propose the split to the developer before you write any new code:

      1. **`/qa-plan sprint` output** — `production/qa/qa-plan-sprint-2.md`
         (untracked) plus the addendum blocks appended to 14 story files.
      2. **The QQ table audit** — `docs/architecture/architecture.md`. QQ-01,
         QQ-02, QQ-04 and QQ-06 moved to a new Resolved table. QQ-03, QQ-05 and
         QQ-07 stay. Two corrections narrow QQ-05.
      3. **GA-002** — `src/scripts/autoloads/gravity_authority.gd`,
         `tests/unit/gravity/gravity_authority_easing_test.gd` and its `.uid`,
         the story file, `production/sprint-status.yaml`, and four new rows in
         `docs/tech-debt-register.md`.

    `tests/smoke/critical-paths.md` also carries a 3-line edit. Read it and
    assign it to one of the three groups.

    Three commits are cleaner than one. The developer decides.

    ## Where things stand

    **Stage: still Pre-Production.** `production/stage.txt` reads
    `Pre-Production`. The 2026-08-25 production gate returned CONCERNS, not PASS,
    and the skill advances the stage only on PASS. Advancing is the developer's
    decision and has not been made.

    **GA-002 is Complete**, closed 2026-08-26, verdict COMPLETE WITH NOTES.
    9 of 9 acceptance criteria pass and every one maps to an asserting test.
    `/code-review` returned APPROVED WITH SUGGESTIONS. Evidence:
    `tests/unit/gravity/gravity_authority_easing_test.gd`, 13 cases.
    The suite went from 178/178 across 11 suites to 191/191 across 12.

    **Sprint 2 rows.** GA-001 and GA-002 are done. These stay:

      - GA-003..GA-007        gravity-authority tail, Must Have
      - LS-001..LS-006        level-state epic, Must Have
      - PP-001 -> LV-005 -> LV-006   Should Have, a strict chain
      - BUG-0002, PROC-1, DEBT-1     Nice to Have

    The GA and LS tracks are independent and can run side by side.

    DO NOT re-run `/gate-check production`, `/retrospective`, `/sprint-plan new`,
    `/qa-plan sprint`, or CI-1. All five are done.

    ## Do these, in this order

    ### 1. Propose the commit split above

    ### 2. `/story-readiness` on GA-003, then `/dev-story`

    `production/epics/gravity-authority/story-003-player-gravity-component-becomes-consumer.md`

    GA-003 makes `PlayerGravityComponent` a consumer of the authority. It carries
    one piece of deferred work from GA-002 that you must not skip.

    **The deferred test deletion is GA-003's job.** The Sprint 2 QA plan addendum
    told GA-002 to delete `test_gravity_lerp_moves_toward_target` and
    `test_gravity_lerp_noop_when_already_at_target` from
    `tests/unit/gravity/gravity_component_test.gd`. GA-002 did not delete them,
    and that was correct: `update_gravity_lerp()` is still live at
    `player_gravity_component.gd:69` and still called from `player.gd:138`, so
    both tests still cover shipping code. GA-002's Out of Scope section assigns
    the removal to GA-003, and it outranks the addendum. **When GA-003 removes
    `update_gravity_lerp()`, delete both tests in the same story and record the
    suite count before and after.** Tech-debt item 4 of 2026-08-26 closes then.

    LS-001 is the other valid starting point and is independent of the GA track.

    ## Open tech debt from GA-002 — four new rows

    All four are in `docs/tech-debt-register.md`, dated 2026-08-26. Two are cheap
    and you may fold them into GA-003 if the developer agrees:

    1. **`direction_ease_rate` has no `@export_range` floor.** Set to 0 or
       negative, `clampf` yields 0, the residual never shrinks, the settle
       epsilon never trips, and the turn freezes silently with no diagnostic.
       Every other externally-supplied value on that node refuses and calls
       `push_error`. The export *declaration* belongs to GA-001, which is why
       GA-002 did not fix it.
    2. **`_settle_steps()` lacks a ceiling assertion** in
       `gravity_authority_easing_test.gd`. It caps at `SETTLE_STEP_CEILING` (240)
       and returns that count without asserting the loop exited via settlement.
       A slow-rate ease that never terminates would return 240, still greater
       than the fast path's 5, and the test would pass on a broken ease. The
       sibling helper `_run_transition()` does assert the ceiling.
    3. **`control-manifest.md:170` is stale.** It budgets the prop wake pass at
       "~6-7 frames per gravity change". The 2.5-degree settle epsilon makes it
       5. **GA-007 sizes its wake pass against this number** — fix it before
       GA-007, not after.
    4. **The deferred test deletion.** Item 2 of the previous section.

    ## Two numbers GA-002 pinned — do not re-derive them

    The direction ease settles at **step 5 = 83.333 ms** for every 90-degree
    change, against GDD AC5's 100 ms. The 180-degree flip settles at step 6 =
    100.0 ms. `DIRECTION_SETTLE_EPSILON` is 2.5 degrees and clears the
    1.9919-degree step-5 residual by 0.50806 degrees.

    The Sprint 2 QA addendum quotes these as "16.7 ms" of headroom and "+0.51
    degrees" of margin. Both round UP. The measured values are 16.667 ms and
    0.50806 degrees, so an assertion floor set at the quoted figure fails a
    correct implementation. The test uses 0.01666 and 0.508. **If you write
    another margin assertion, compute the measured value. Do not copy the prose.**

    ## The gate's ranked concerns — do not re-derive these

    All eleven are in `production/gate-checks/gate-check-2026-08-25-production.md`
    with evidence. The top three that still matter to Sprint 2:

    1. **QQ-03 — the 8 built levels do not implement the current design, and no
       epic owns migrating them.** No `level_01..08.tscn` declares
       `default_gravity` or `oxygen_capacity`. Does not block Sprint 2
       (Foundation only, authors no levels); compounds if new level content is
       authored first.
    2. **CI guards do not reach the trunk.** See the open question below.
    3. **The four slice legibility gaps**, carried as override entry conditions
       on the gate's CONCERNS verdict.

    ## One open question for the developer

    **Merge `vertical-slice` into `development`?** The branch is 78 commits ahead
    and unmerged, and the three uncommitted groups above will push it further.
    `.github/` does not exist on `development` at all, so the four ADR guards —
    proven, all four fired in one run against five planted violations — do not
    guard the trunk. This is the developer's call. There is no `gh` CLI on this
    machine, so opening a PR is a browser action.

    ## Stale claims corrected — do not reintroduce them

    1. **"The suite is 178/178" is WRONG.** It is 191/191 across 12 suites.
    2. **"Sprint 2 has no QA plan" is WRONG.** `/qa-plan sprint` ran on
       2026-08-25. The plan is at `production/qa/qa-plan-sprint-2.md`.
    3. **"Sprint 2 starts 2026-09-01 and has not begun" is WRONG.**
       Implementation started early. GA-001 and GA-002 are done.
    4. **"Strike the four stale QQ rows" is DONE**, uncommitted, in
       `docs/architecture/architecture.md`.
    5. **"No epic covers physics props" is WRONG.** The `physics-props` epic was
       created 2026-08-24 in `c53c421` and decomposed in `4c5572c`.
    6. **"The gate is blocked on CI-1" is WRONG.** Both are done.

    ## Working rules

    - Follow the collaboration protocol in `CLAUDE.md`. Ask before Write or Edit,
      show the changeset, do not commit unless told.
    - Review mode is `lean` (`production/review-mode.txt`). `/story-done` skips
      the QA coverage gate and asks whether `/code-review` was run.
    - **NEVER `git add -A`.** It swept an unrelated level edit into a docs commit
      on 2026-08-25. Stage named paths and read the diff. If Godot is open,
      expect scene files to drift.
    - **Run `--import` after you write a new test file.** It generates the `.uid`
      sidecar. Every tracked test file has one, and a clean CI checkout needs it.
      GA-002's was missed at implementation and caught at code review.
    - **Never tick a blocking AC on inference.** Open the story file and read its
      Test Evidence status.
    - **An empty `blocker` field is not proof of no gate**, and a `Blocked` status
      header is not proof of a live gate. LV-005 and LV-006 both carry stale
      `Blocked` headers while scheduled inside Sprint 2.
    - When reality and an approved AC diverge, **annotate the AC**. Do not
      silently tick it and do not reword the requirement.
    - **Before you relay a subagent's severity that depends on project config,
      open the config file yourself.** `project.godot` has no
      `debug/gdscript/warnings/*` section. That invalidated four BLOCKING
      findings in Sprint 1, and a fifth on 2026-08-26: a specialist reported that
      misplaced `@warning_ignore` annotations would fail gdUnit4 test discovery.
      The misplacement is real. The BLOCKING severity was invented — the suite is
      green, and the identical placement exists in all seven other test files.
    - **Where a spec states a duration, tolerance or floor, compute the MARGIN**,
      not just whether it passes.
    - **When a QA plan and a story's Out of Scope section disagree, the story
      wins**, and you record the deviation. This happened on GA-002 and the
      addendum was the wrong document.
    - **Two OPEN 4.7.1 engine unknowns sit in Sprint 2's scope.** GA-006 carries
      ADR-0001 Verification 2 (does a default-space write reach every
      `RigidBody2D` in the same step?). LV-006 carries the headless `Area2D`
      extent read on a node never added to a tree. Verify each against the binary
      before you build on it. GA-006 has a named fallback.
    - **The windowed Godot editor segfaults on this machine.** The route around
      it is the read-only headless probe at
      `production/qa/evidence/editor-facts-probe-2026-08-25.md`. It proves the
      data, not the rendering. Reuse it. Do not re-derive it.
    - Static typing everywhere. gdUnit4 treats GDScript warnings as errors at
      discovery, so one warning anywhere fails the whole suite.
    - Use the Write tool, not Bash heredocs, for any file with apostrophes or
      long prose. Heredocs have mangled two sessions' worth of writes.
    - **`session-logs/` was deleted on 2026-08-25 by an unidentified cause** and
      is gitignored. `active.md` was reconstructed and states its own provenance.
      The tracked record is the source of truth: `production/gate-checks/`,
      `production/sprints/`, `production/qa/`, `production/retrospectives/`,
      `production/epics/`, `docs/tech-debt-register.md`, and the git log. Do not
      reconstruct lost history from inference.

## Copy to here

---

## Shorter variant

    Resume on `vertical-slice`. Sprint 2 is IN PROGRESS — GA-001 and GA-002 are
    Complete. `/qa-plan sprint` has been RUN (`production/qa/qa-plan-sprint-2.md`).
    The suite is **191/191 across 12 suites**, not the 178/178 every older prompt
    states. Nothing from the last three sessions is committed: 20 modified files
    and 2 untracked, spanning the QA plan, the `architecture.md` QQ audit, and
    GA-002. Propose a three-commit split first. Then run `/story-readiness` on
    `production/epics/gravity-authority/story-003-player-gravity-component-becomes-consumer.md`
    and `/dev-story`. GA-003 must delete the two `test_gravity_lerp_*` cases from
    `gravity_component_test.gd` when it removes `update_gravity_lerp()` — GA-002
    correctly deferred that, because the method is still live at
    `player_gravity_component.gd:69`. Read the four new 2026-08-26 rows in
    `docs/tech-debt-register.md`; item 3 (the stale "~6-7 frames" line at
    `control-manifest.md:170`, actually 5) must be fixed before GA-007 sizes its
    wake pass against it. Review mode is `lean`. Ask before writing. Never
    `git add -A`.

## Quick map of what is open

| Item | Type | Blocks |
|---|---|---|
| Commit the 3 uncommitted groups | agent work, needs approval | nothing mechanical; risk grows with the backlog |
| GA-003 `/dev-story` | agent work | GA-004…GA-007 |
| Delete the 2 `test_gravity_lerp_*` cases | inside GA-003 | tech-debt item 4 |
| Fix `control-manifest.md:170` (6-7 → 5 frames) | agent work, ~1 line | GA-007 sizing its wake pass |
| `direction_ease_rate` range floor | tech debt, cheap | nothing |
| `_settle_steps()` ceiling assertion | tech debt, cheap | nothing |
| LS-001…LS-006 | agent work, independent track | Foundation completion |
| PP-001 → LV-005 → LV-006 | agent work, strict chain | LV-006 |
| Advance `production/stage.txt` to `Production` | **human** — gate was CONCERNS | nothing mechanical |
| Merge `vertical-slice` into `development` | **human** — browser PR, 78+ commits | CI guards reaching the trunk |
| QQ-03 — level migration epic | unowned | pillars 2 and 3 being true of the built game |
| The 4 override entry conditions | mixed | any Sprint 3 content work |
| `/create-control-manifest update` | owed | `oxygen-drain` story 006, Sprint 3 |
| `/review-all-gdds` + `/design-review` ×6 | never run, no owner | nothing formally |
| BUG-0002 (S4) | Sprint 2, nice-to-have | nothing |
| 16 tech-debt items | backlog, no owners | nothing |
